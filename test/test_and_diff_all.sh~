rm *.ref.out -f
rm *.ref.errors -f
FILES="./*.cl"
for f in $FILES
do
    echo "reference semanting '$f'"
    /usr/class/cs143/bin/coolc -k "$f" > "$f.ref.errors" 2>&1
    rm "lexer.out" -f
    rm "parser.out" -f
    mv "semant.out" "$f.ref.out"
done
rm *.s -f